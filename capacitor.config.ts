import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'parki_admin_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
